<% html_title t(:label_roadmap) %>





<% if @kittens.empty? %>
  <!-- <%= no_results_box %>  -->
<% else %>
  <p>
    These are the kittens:
  </p>
  <% @kittens.each do |kitten| %>
    <%= angular_component_tag 'opce-kitten', inputs: { 'kitten-name': kitten.name } %>
  <% end %>
<% end %>

  <table class="">
    <th>
      <td></td>
    <% @roadmap_hashes.each do |m_hash| %>
      <td class="">
        <%= m_hash[:label] %>
      </td>
    <% end %>
    </th>
    <% @table_rows.each do |label| %>
    <tr>
      <td><%= label %></td>
      <% @roadmap_hashes.each do |m_hash| %>
      <td>
        <%m_hash[label].each do |wp|%>
          <div class="box">
            <%= wp.subject %>
          </div>
        <%end%>
      </td>
      <%end%>
    </tr>
    
    <% end %>
    
  </table>


<!-- <%= render "shared/kitty_image" %> -->
